\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amsthm}
\usepackage[ngerman]{datetime}
\usepackage{geometry}
\usepackage[dvipsnames]{xcolor}
\usepackage{cleveref}
\usepackage{enumerate}

%  hyperref aktivieren, wenn fertig!!!
% \usepackage{hyperref}
% \usepackage{cleveref}

% Literaturverzeichnis
\usepackage[backend=biber,style=alphabetic]{biblatex}
\addbibresource{bibliography.bib}


%%%%%
% shortcut for green textcolor
\newcommand{\gext}[1]{\textcolor{ForestGreen}{#1}}

% define refernece above "=" sign:
\newcounter{relctr} %% <- counter for relations
\everydisplay\expandafter{\the\everydisplay\setcounter{relctr}{0}} %% <- reset every eq

\newcommand\oben[1]{%
    \refstepcounter{relctr}%
    \stackrel{\textnormal{(\alph{relctr})}}{\mathstrut{#1}}%
}
\AtBeginDocument{\let\originallabel\label}%% <- store original definition
%%%%%

% define \Ima for image
\DeclareMathOperator{\Ima}{Im}
